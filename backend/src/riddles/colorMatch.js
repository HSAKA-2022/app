import { riddle } from "../riddle"

const riddleId = "colorMatch"

export default riddle({
    riddleId,
    start: (players) => {

    },
    solved: (players) => {
    },
    getter: (players) => {
    },
    phoneActions: {
    },
    piActions: {
    },
})
